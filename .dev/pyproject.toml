[project]
name = "test-project"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = []

[dependency-groups]
dev = ["pytest==8.4.2", "ruff==0.14.1"]
heavy-rarely-updated = ["torch==2.9.0"]
light-frequently-updated = ["fastapi==0.119.1", "uvicorn==0.38.0"]

[tool.uv]
default-groups = ["dev", "heavy-rarely-updated", "light-frequently-updated"]
